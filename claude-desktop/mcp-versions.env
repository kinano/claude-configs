# MCP server version pins — bump here when upgrading
REDSHIFT_MCP_VERSION=0.0.19

# DBT_MCP_VERSION is intentionally unpinned — dbt-mcp follows latest by default
# To pin: add DBT_MCP_VERSION=x.x.x here and update dbt-mcp.sh accordingly
